{
  "title": "Core",
  "description": "Core values for RPGit",
  "type": "object",
  "properties": {
    "gauges": {
      "type": "array",
      "title": "Gauges",
      "format": "tabs",
      "uniqueItems": true,
      "items": {
        "$ref": "watch/gauge.json"
      },
      "options": {
        "disable_array_reorder": true
      }
    },
    "sets": {
      "type": "array",
      "title": "Sets",
      "format": "tabs",
      "uniqueItems": true,
      "items": {
        "$ref": "watch/set.json"
      },
      "options": {
        "disable_array_reorder": true,
        "disable_array_delete": true
      }
    }
  },
  "required": [
    "gauges",
    "sets"
  ]
}
